application:
  github-auth:
    token: testAccessToken
  animedia:
    search-list-max-size: 2
    request-limit: 2
  mal:
    offset-step: 2

animedia-api-feign-client:
  ribbon:
    listOfServers: localhost:${wiremock.server.port}

animedia-site-feign-client:
  ribbon:
    listOfServers: localhost:${wiremock.server.port}

mal-feign-client:
  ribbon:
    listOfServers: localhost:${wiremock.server.port}

github-feign-client:
  ribbon:
    listOfServers: localhost:${wiremock.server.port}

nine-anime-feign-client:
  ribbon:
    listOfServers: localhost:${wiremock.server.port}