starter-common:
  github:
    token: foobar
    url: http://localhost:${wiremock.server.port}/
  external-services:
    mal-access-service-url: http://localhost:${wiremock.server.port}/

starter-reactive-services:
  web-client:
    max-attempts: 0

starter-cache:
  type: CAFFEINE

application:
  cache-props:
    cache-on-startup: false
  mal-props:
    offset-step: 3
    url: http://localhost:${wiremock.server.port}
  enabled-fandub-sources:
    - ANI_DUB
    - ANI_LIBRIA
    - ANI_WAVE

logging:
  level:
    ROOT: INFO
    nasirov.yv: DEBUG
    reactor.netty: ERROR